[buildout]
extends = buildout.cfg

[erp_global]
project = <project_name>
version = 7.0
current_instance = openerp_70_${source_profile:profile}_${erp_global:project}

[source_profile]
# trunk or stable : used for the path of sources branches
profile = trunk

[source_repository]
path_specific = sftp://bazaar.camptocamp.net/srv/bzr/${erp_global:project}_openerp/${source_profile:profile}

[openerp]
options.xmlrpc_port = 8101
options.db_password = ooL6oew55
options.running_env = dev
gunicorn = direct
gunicorn.workers = 1

openerp-web.company.url = 'http://www.camptocamp.com/'
